public enum GameState { menu, lobby, running, paused }
public enum EnemyState { moving, attacking, dying }
public enum SpawnPointState {prewave, wave, postwave }
public enum UnitState {idle, shooting, dying}
public enum PlayerState { idle, placing, relocating, vaccinating, healing }
public enum LevelState { wave, betweenWave }
public enum DashType {straight, hight, combined}